<%= form_with(model: tv_series, local: true) do |form| %>
  <% if tv_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tv_series.errors.count, "error") %> prohibited this tv_series from being saved:</h2>

      <ul>
        <% tv_series.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :season %>
    <%= form.text_field :season %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
